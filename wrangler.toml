name = "heck-holdings"
compatibility_date = "2026-01-01"
pages_build_output_dir = "dist"

# Rate limiting for /api/agent-spec is enforced in code using the runtime
# Cache API (no binding required). See functions/api/agent-spec.js.
